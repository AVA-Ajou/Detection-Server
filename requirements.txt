# 맥(MPS)에서는 bitsandbytes를 쓰지 않는다 — CUDA 전용이라 설치해도 못 쓴다.
# GPU 서버에 올릴 때만 `pip install bitsandbytes` 를 추가한다.
anthropic>=0.40
torch>=2.5
transformers>=4.50
peft>=0.14
accelerate>=1.0
fastapi>=0.115
uvicorn>=0.32
python-multipart>=0.0.9    # /transcribe 의 파일 업로드

# 오디오·이미지 전처리기. Gemma 4 의 프로세서가 요구한다 —
# 텍스트만 쓸 생각이어도 프로세서를 열려면 있어야 한다.
librosa>=0.10
soundfile>=0.12
pillow>=10
torchvision>=0.20
